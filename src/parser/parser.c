#include "ft_defines.h"
#include "ft_lexer.h"

static t_ast_node *parse_tokens(t_list *tokens)
{
	return NULL;
}

void	run_parser(t_list *tokens, char *input)
{
	t_ast_node *ast;
	
	ast = parse_tokens(tokens);
}